<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>PUZMAGE Ingresar</title>
  <%- include('../partials/head'); %>
  <link rel="stylesheet" href="/css/home.css">
  <link rel="stylesheet" href="/css/login.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
</head>

<body>
  <div class="login-container">
    <div class="login-card glass">
      <div class="card-header text-center">
        <h4 class="login-title">
          <i class="bi bi-lock-fill me-2"></i> Iniciar Sesión Puzz
        </h4>
      </div>
      <div class="card-body">
        <form action="/login" method="post">
          <div class="form-group mb-4">
            <label class="form-label text-start w-100 fw-semibold">Usuario o Correo Electrónico</label>
            <div class="input-icon">
              <i class="bi bi-person-fill"></i>
              <input type="email" name="usu_email" class="form-control" placeholder="ejemplo@correo.com" required />
            </div>
          </div>

          <div class="form-group mb-4">
            <label class="form-label text-start w-100 fw-semibold">Contraseña</label>
            <div class="input-icon">
              <i class="bi bi-lock-fill"></i>
              <input type="password" name="usu_password" class="form-control" placeholder="********" required />
            </div>
          </div>

          <button class="btn btn-primary w-100 fw-bold mt-3">INGRESAR</button>
        </form>

        <% if (typeof msg_e != "undefined") { %>
          <div class="text-danger text-center fw-semibold mt-3"><%= msg_e %></div>
        <% } %>
      </div>
    </div>
  </div>
</body>

</html>
